[package]
name = "pokedata_api_build"
version = "0.1.0"
edition = "2021"

[dependencies]
pokedata_api_entities = { path = "../entities" }
pokedata_api_parsing = { path = "../parsing" }
pokedata_api_utils = { path = "../utils" }
reqwest = { version = "0.12.7", features = ["blocking"] }
serde = { version = "1.0.210", features = ["derive"] }
serde_json = "1.0.128"
